#include <bits/stdc++.h>
using namespace std;

inline bool isbiton(register ull n, register int k)
{
    return (n & (1<<k)) != 0;
}

int main()
{
    int k;
    cin >> k;
    while (k!=0 )
    {
        cout << (1<<(k-1)))
    }
}
