#include <bits/stdc++.h>
using namespace std;

int main()
{
    int i,t,y;
    cin t;
    int n[t],m[t];
    for(i=0;i<t;i++)
    {
        cin >> n[i] >> m[i];
        int a[n[i]] , b[m[i]];
         for(y=0 ; y< n[i];y++)
         {
             cin >> a[y] >> b[y];
         }
    }
}
