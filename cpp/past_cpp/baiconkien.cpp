#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int m ,i, n , a[100],b ,c,p[100],s;
    cin >> m >> n;
    s = m + n;
    if(n < m)
    {b = m ;
    m = n;
    n = b;}
    for (i = 1 ; i <= s ; i++)
    {
        for(b = 0 ; s <= i ; b++)
        {
            p[i] =
            a[j] =
        }
    }
}
