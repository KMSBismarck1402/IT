#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a,b,c,s,i;
    cout << "nhap vao 1 so nguyen random ";
    cin >> a;
    s = 1;
    b =a;
    while( b > 10)
    {
        b = b/10;
        s = s+1;
    }
    b = a;
    int d[s];
    for (i= 1 ; i <= s ; i ++)
    {
        if
        b = a/10;
    }
    b =a ;
    for (i = 1 ; i <= s ; i++)
    {
        if(d[i]%2 == 0)
        {
            b = d[i
        }
    }
}
